<br><div class="content"><h1>Employee Pay By Date</h1>

  <%= form_tag ("create2") do %>

     <div class ="field"> <%= label "dates","start_date" %> <%= text_field "dates","start_date" %></div>
      <div class ="field"> <%= label "dates","end_date" %><%= text_field "dates","end_date" %></div>
        <div class ="field"> <%= label "employees","employee_id" %> <%= collection_select :employees, :employee_id, Employee.all, :id, :first_last_name, :prompt => true %>
        </div>
      <%= button_tag("Submit") %>

     <br><br> <%= link_to 'Back', reports_path %>
  <% end %>

  <% unless @appointments.nil? %><p> The selected employee has made
      <%
        sum =0



        @appointments.each do |app| %>

            <%=  (sum += app.activity.price)

                if app == @appointments.last





              number_to_currency(sum*0.50)




            end%>



          <%end%>
  during the selected time period. <%

   end %>
  </p>
</div>